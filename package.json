{
  "name": "protobuf-decoder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "vite",
    "build": "vite build --outDir build",
    "test": "vitest --run --globals --reporter verbose",
    "test:coverage": "vitest --run --globals --reporter verbose --coverage",
    "test:watch": "vitest --globals --reporter verbose --ui"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/react": "^15.0.4",
    "@vitejs/plugin-react-swc": "^3.8.0",
    "@vitest/coverage-v8": "^3.0.8",
    "@vitest/ui": "^3.0.8",
    "husky": ">=1",
    "jsdom": "^24.0.0",
    "lint-staged": ">=8 <=15",
    "prettier": "1.18.2",
    "react-dom": "^18.3.0",
    "vite": "^6.3.6",
    "vitest": "^3.0.8"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,css,json,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "jsbi": "^4.3.0",
    "react": "^18.3.0",
    "semantic-ui-css": "^2.5.0",
    "semantic-ui-react": "^2.1.5"
  }
}
